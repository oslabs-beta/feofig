const lazyLoadConfig = {
  lazyload: {
    threshold: 0.5, // Customize as needed
    once: false, // Set to true or false as per your requirement
    // You can add more LazyLoad props here
  },
};

export default lazyLoadConfig